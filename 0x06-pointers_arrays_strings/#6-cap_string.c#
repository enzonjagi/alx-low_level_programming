#include "holberton.h"

/**
 * *cap_string - capitalizes each word of a string
 * @str: pointer to string received
 *
 * Return: str
 */

char *cap_string(char *str)
{
	int i, j;
	char sep[] = {',', ';', '.', '!', '?', '"', '(', ')', '{', '}'};

	j])
			{
				++i;
				if (str[i] >= 'a' && str[i] <= 'z')
				{
					str[i] = str[i] - 32;
				}
			}
		}
	}
	return (str);
}
