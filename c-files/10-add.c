#include "holberton.h"

/**
 * add - this function adds of two numbers
 * @a: the integer value it receives
 * @b: another int parameter of the function
 *
 * description:
 * add()
 * the function receives two integers a and b
 * then adds them and returns the result
 * Return: int a + int b
 */

int add(int a, int b)
{
return (a + b);
}
